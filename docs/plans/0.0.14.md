# 0.0.14 Plan: 组装对话上下文

> 注：本文件为历史计划记录，实际实现以当前代码与文档为准。

## 目标

组装 system_prompt（包含 MCP usage method）与历史上下文；skills 作为自动挂载基建。

## 关键任务

### 1) system_prompt 组装

- [x] 从 group agent.md 读取 system_prompt
- [x] 拼接 MCP usage method 到 system_prompt

### 2) history 注入

- [x] 把 history 作为上下文传给 opencode

### 3) skills 挂载

- [x] skills 由启动路径自动挂载，不在 prompt 中手动拼接

## 验证计划

- [ ] 手动：system_prompt 中包含 MCP usage method
- [ ] 手动：history 注入后响应可用
