# 0.0.15 Plan: 关键路径测试补齐

## 目标

补齐 opencode runner 与触发逻辑的关键路径测试，避免回归。

## 关键任务

### 1) runner 输出解析测试

- [x] 覆盖 JSON 输出中的 `messages/history` 解析
- [x] 覆盖 `output/content` 提取逻辑

### 2) 触发逻辑测试

- [x] mention/keyword/all 的触发判断
- [x] cooldown 与 adminUsers 的行为

## 验证计划

- [x] `bun test src` 通过
