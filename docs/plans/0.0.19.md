# 0.0.19 Plan: K8s 命名空间与部署约束收敛

> 注：本文件为历史计划记录，实际实现以当前代码与文档为准。

## 目标

- 统一 K8s 资源命名空间与存储命名
- 强制 WebUI Token 配置，避免空值部署
- 补齐 pmhq 与 llbot 的集群内连接资源

## 关键任务

- [x] 命名空间统一为 `bot` 并同步 PVC 命名
- [x] Secret 生成脚本强制 `WEBUI_TOKEN` 非空
- [x] llbot 启动脚本强制 `WEBUI_TOKEN` 非空
- [x] 增加 pmhq Service 与端口声明
- [x] 更新部署与快速开始文档

## 验证计划

- [x] `kubectl apply -f deployments/k8s/*.yaml` 在 `bot` 命名空间可用
